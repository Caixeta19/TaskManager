spring:
  application:
    name: taskManager
  datasource: #configuração database
    url: jdbc:mysql://localhost:3306/taskmanager?useSSL=false&serverTimezone=UTC
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa: #configurações do Jpa
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true